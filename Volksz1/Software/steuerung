 
#power gpio 17
# f10c0200-d354-11e9-9c59-cb1e198abaf0

# pv-anlage gpio 18
# 6ab67750-d5b4-11e9-81fd-d7cc4b777585


http://192.168.1.55/middleware.php/data/6ab67750-d5b4-11e9-81fd-d7cc4b777585.json?operation=add&value=10




http://192.168.1.55/?uuid=6ab67750-d5b4-11e9-81fd-d7cc4b777585

# konfig datei
# /etc/vzlogger.conf

# vzlogger starten  für Fehlermeldungen auszulesen
# cd /usr/local/bin/
# ./vzlogger

# Problem kein netzwerkzugriff auf internet:
# sudo route add default gw 192.168.1.1

# Den vzlogger kann man dann mit dem üblichen systemctl Kommando starten, stoppen und prüfen:
#
# sudo systemctl start vzlogger
# systemctl status vzlogger
# sudo systemctl stop vzlogger

# setzen feste netzwerkadressen
# sudo nano /etc/dhcpcd.conf

# mysql zugriff auf datenbank
# sudo mysql --user=root -pwtrvNJsecH3
# use volkszaehler;

show tables from volkszaehler;
+------------------------+
| Tables_in_volkszaehler |
+------------------------+
| aggregate              |
| data                   |
| entities               |
| entities_in_aggregator |
| properties             |
+------------------------+
# Datenbank löschen-vorsicht,mm dann muss install.sh ausgeführt werden.

# mysql> DROP DATABASE db_name;
